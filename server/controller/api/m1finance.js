const express = require('express');
const router = express.Router();
//const m1financeCustom = require('../coinbase/m1financeCustom.js');

router.get('/accounts', (req, res) => {
// m1financeCustom.getAccountsCustom(res);
});

module.exports = router;
